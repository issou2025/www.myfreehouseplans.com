# Django
SECRET_KEY=replace-me
DEBUG=False
ALLOWED_HOSTS=www.myfreehouseplans.com,localhost,127.0.0.1

# Database (Render Postgres will provide DATABASE_URL)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# Optional Gumroad API key (only if server-side API calls are enabled)
GUMROAD_API_KEY=

# Optional email (only if you enable SMTP in production)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=